# --- Server ---
NODE_ENV=development
PORT=4000
LOG_LEVEL=info

# --- Database ---
DATABASE_URL=postgresql://lactasync:lactasync@localhost:5432/lactasync?schema=public

# --- Auth ---
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d
BCRYPT_ROUNDS=12

# --- Anthropic / Claude ---
ANTHROPIC_API_KEY=sk-ant-...
CLAUDE_MODEL=claude-sonnet-4-6

# --- Twilio WhatsApp ---
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
TWILIO_VALIDATE_SIGNATURE=true
PUBLIC_BASE_URL=https://your-domain.example.com

# --- Guidance defaults ---
# Fallback interval (hours) used when there's no recent history. The live
# interval is personalized as 72 / (feedings in the last 3 days).
FEEDING_INTERVAL_HOURS=3

# --- Daily WhatsApp summary ---
DAILY_SUMMARY_ENABLED=true
DAILY_SUMMARY_HOUR=22
DAILY_SUMMARY_TIMEZONE=Asia/Jerusalem
